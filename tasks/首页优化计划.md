* page 组件中，头部的 logo 图片，需要放大一些，我看现在 log 在 div 内，而 外层容器 设置了内边距，你把内边距移除，然后只把内边距加到右侧 搜索框，和登录注册的 容器上
    * logo 右侧的文字重复了，需要去掉：全国农作物种质资源信息平台，以及下面的英文标题

* 首页轮播图 App\Livewire\Components\Index\Featured
    * 左侧轮播图，轮播图上显示 标题，描述，和 分类；分类作为标签显示，替换现在写死的："权威数据, 标准规范, 安全可靠, 开放共享"，分类获取方式可参考app\Livewire\Components\Index\ScientificResearch.php 组件并且分类要全显示，而不是只显示第一个
    * 右侧文章列表，可否只显示 4条，当屏幕变大变小，导致右侧内容高度变化时，改变每条文章的高度来自适应
        * 我之前的 vendor\wsmallnews\cms\src\Livewire\Components\Post\IndexPosts.php 组件是支持的，不过因为我动态控制了描述字段的显示，导致不完美；本组件本来就不显示 描述字段，所以应该可以直接处理好
    * 右侧文章列表顶部的分类，现在出现了一个很短的竖向滚动条， 能滚动一点点点，这个要完全去掉，不可以出现竖向滚动条；当宽度不够时，出现横向滚动条，这个 ok
* 人才储备， 服务案例，科研成果等，区块的间距过大，显得有点空

* 科研成果显示的样式，和我最原始提供的 png 图不一样， 最主要的区别是没有内边距，左侧的图片是直接贴着上下左，三边的边框的， 并且鼠标经过，图片有放大效果

* 页面 footer 组件重写 flex 布局，分三部分，左侧，中间，右侧，lg 以上时，全部显示，并且横向排列，md 以上时，只显示中间区域，和右侧区域，sm 以上时，将 flex 改为竖排，但左侧依然不显示，只显示中间和右侧
    * 左侧显示 header 中的那个logo,当在 lg 以上时显示
    * 中间区域显示所有除了 NavigationType::Child 类型的导航项，不分层级平铺展示，使用 grid 布局根据页面宽度自适应每行显示条数，左侧 header 可能会消失，右侧 联系我们也会因为 flex 改为竖排单独一行
        * 中间区域采用 tailwind 的 Container queries，根据容器宽度自适应每行显示条数
    * 右侧联系我们复用  cms 的 footer 的右侧内容
    * 最下面的版权信息，采用主题色作为背景，内容保持 cms 的 footer 内容
    